node_modules
dist
.env
.DS_Store
.claude/
.playwright-mcp/

# generated / dev scratch (ponytail: kept on disk, just out of git's way)
coverage/
/*.png
/_*

# large Power BI binary + raw source data (exceed GitHub's 100MB limit)
# .pbix ships via GitHub Release, never via the repo
*.pbix
powerbi/p1/project1_data/

# Power BI project local state (data cache, user settings)
powerbi/p1/p1.SemanticModel/.pbi/
powerbi/p1/p1.Report/.pbi/

# brainstorm visual companion
.superpowers/
